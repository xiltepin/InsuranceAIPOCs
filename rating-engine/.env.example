# Database environment — copy to .env and fill in your values
DB_HOST=localhost
DB_PORT=5432
DB_NAME=insurance_poc
DB_USER=postgres
DB_PASSWORD=postgres
DB_POOL_SIZE=8

# How many rows to sample from DB for RF training (1M is fast, 5M is more accurate)
RF_TRAINING_SAMPLE=1000000
